{
  "name": "@aiview/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "dev": "wait-on tcp:127.0.0.1:5173 && npm run build && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron ."
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "cross-env": "^7.0.3",
    "electron": "^43.0.0",
    "typescript": "^5.5.0",
    "wait-on": "^8.0.0"
  }
}

# Dev-only overrides — real API keys live in the encrypted vault (safeStorage), never in env (TDD.md §9)

# Path to python executable for the engine sidecar (default: engine/.venv, then PATH)
#AIVIEW_PYTHON=

# SQLite path override for the engine (default: Electron userData/aiview.sqlite3)
#AIVIEW_DB=
